{
  "name": "eternal-ricochet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "check:src": "node scripts/check-src.mjs",
    "smoke:logic": "node scripts/smoke-core.mjs",
    "smoke:assets": "node scripts/smoke-assets.mjs",
    "smoke:pwa": "node scripts/smoke-pwa.mjs",
    "smoke:release": "node scripts/smoke-release-gates.mjs",
    "validate": "npm run check:src && npm run smoke:logic && npm run build && npm run smoke:assets && npm run smoke:release && npm run smoke:pwa",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  }
}
